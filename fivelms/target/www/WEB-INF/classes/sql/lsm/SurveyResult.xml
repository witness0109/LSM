<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.kosmo.jobkorea.lsm.dao.SurveyResultDao">

	<!-- 설문결과 리스트 조회 -->
<!-- 	<select id="surveyList" resultType="kr.kosmo.jobkorea.lsm.model.SurveyResultModel">
		SELECT 	a.lec_seq, 
				a.sv_seq, 
				b.lec_nm, 
				a.sv_score, 
				a.enr_user,
				a.enr_date
		FROM tb_survey a
		JOIN tb_lecture b on a.lec_seq = b.lec_seq;
		<where>
			<if test="searchKeyword != null and !='' ">
				${searchKeyword} like '%${searchKeyword}%'
			</if>
		</where>
		ORDER BY lec_seq DESC
		LIMIT #{pageIndex}, #{pageSize}
	</select>

	설문결과 총 갯수 조회

	<select id="countSurveyList" resultType="int">
		SELECT COUNT(*) AS toCnt from tb_survey
	</select>
	
	설문결과 단건 조회
	<select id="selectSurveyDetail" resultType="kr.kosmo.jobkorea.lsm.model.SurveyResultModel">
		SELECT 	a.lec_seq,
				a.sv_seq,
				b.lec_nm,
				a.sv_score,
				a.enr_user,
				a.enr_date
		FROM tb_survey a
			JOIN tb_lecture b on a.lec_seq = b.lec_seq
			WHERE a.lec_seq = #{lec_seq}
		ORDER BY a.lec_seq DESC
	</select> -->
</mapper>